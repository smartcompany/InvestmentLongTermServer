name: AI trend push

on:
  schedule:
    # Daily 00:00 UTC (09:00 KST)
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Call ai-trend-push cron API
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$CRON_SECRET" ]; then
            echo "CRON_SECRET is not set in the production environment"
            exit 1
          fi

          API_BASE_URL="${{ vars.API_BASE_URL }}"
          if [ -z "$API_BASE_URL" ]; then
            API_BASE_URL="https://investment-long-term-server.vercel.app"
          fi

          response=$(curl -sS -w "\n%{http_code}" \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "${API_BASE_URL}/api/cron/ai-trend-push")
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')

          echo "$body"

          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "Request failed with status ${http_code}"
            exit 1
          fi
